import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  height: 35px;
  padding:5px;
  float:right;
  margin: 0 auto;
  display: inline;
`;

export default Img;
